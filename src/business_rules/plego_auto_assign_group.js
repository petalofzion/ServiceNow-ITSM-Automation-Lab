(function executeRule(current, previous /*null when async*/) {
  // TODO: Auto-populate assignment group based on category/urgency.
  // Use PlegoUtils.getAssignmentGroup for logic.
})(current, previous);
